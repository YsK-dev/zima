# Ultra-lightweight Qwen 4B configuration
# This reduces memory usage and prevents overheating

FROM qwen3:4b

# Reduce context window (default is 2048)
PARAMETER num_ctx 512

# Lower temperature for more deterministic output (less compute)
PARAMETER temperature 0.7

# Reduce threads to prevent CPU overload
PARAMETER num_thread 8

# Disable GPU completely (run on CPU only)
PARAMETER num_gpu 0

# System prompt
SYSTEM """You are an AI Geriatric Health Assistant. Provide safe, simple medical advice for elderly patients (70+)."""
